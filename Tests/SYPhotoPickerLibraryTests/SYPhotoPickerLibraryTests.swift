import XCTest
@testable import SYPhotoPickerLibrary

final class SYPhotoPickerLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SYPhotoPickerLibrary().text, "Hello, World!")
    }
}
